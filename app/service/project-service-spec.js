describe('projectService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(projectService) {

    //expect(projectService.doSomething()).toEqual('something');

  }));

});
